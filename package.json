{
  "name": "echo-listen-together",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted ECHO listening rooms and bounded Opus relay",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test test/*.test.mjs",
    "setup": "node scripts/setup.mjs"
  },
  "dependencies": {
    "ws": "8.21.3"
  }
}
